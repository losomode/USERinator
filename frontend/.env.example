# Deployment Configuration (optional)
# Set this when hosting behind a custom domain (e.g. inatorapp.com).
# The Vite dev server will then accept requests from that host and localhost:8080 simultaneously.
# Leave empty for local dev only.
VITE_DEPLOY_DOMAIN=
